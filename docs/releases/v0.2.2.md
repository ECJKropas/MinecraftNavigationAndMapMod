# Wayfarer v0.2.2

> 发布日期：2026-07-22

## 新增

### 道路轨迹简化算法

录制道路时自动对原始轨迹点做两阶段后处理，大幅减少存储点数并消除采集中的冗余路径。

- **回退路径剪枝**：去除物理采集中"走了又回头"产生的 Z 字形冗余。当新采集点回访到历史旧点附近时，自动裁剪中间折返段。
- **Douglas-Peucker 曲线简化**：在保持道路几何形状的前提下，用最少的关键点表示道路折线。容差可在设置中调整。

### 新增配置项

- **RDP 简化容差**（`rdpEpsilon`）：控制曲线简化强度，默认 1.0 格，范围 0.1~10.0。值越大保留点越少、道路越"粗糙"；值越小保留点越多、道路越精细。

## 技术细节

- 简化在录制保存和追加保存时自动执行，不影响已存储的道路数据
- 简化在端点吸附（snapEndpoints）和交叉口检测之前执行，确保几何精度
- 仅操作 XZ 平面（水平面），保留原始高度（Y）和 tick 信息
- 三版本（1.20.1 / 26.1.1 / 26.2）同步支持

## 文件变更

| 文件 | 操作 |
|---|---|
| `src/.../road/RoadSimplifier.java` | 新增 |
| `src/.../road/RoadRecordingManager.java` | 修改 |
| `src/.../WayfarerConfig.java` | 修改 |
| `src/.../config/WayfarerConfigs.java` | 修改 |
| `versions/26.2/.../WayfarerConfig.java` | 修改 |
| `resources/.../lang/en_us.yml` | 修改 |
| `resources/.../lang/zh_cn.yml` | 修改 |
| `docs/road-simplifier-plan.md` | 新增 |
