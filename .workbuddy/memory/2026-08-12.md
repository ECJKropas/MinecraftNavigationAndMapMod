# 2026-08-12

## 前端玻璃透明度微调
- 用户确认 backdrop-filter 实际生效（游戏内浏览器支持），但觉得右上角信息栏(#editor-panel)不够透。
- 改动 src/main/resources/web/index.html：
  - `--glass-bg` 由 0.62 → 0.55（整体玻璃略透）。
  - `#editor-panel` 单独设为 `rgba(255,255,255,0.42)`，比侧边栏更透，方便看到背后模糊路网。
- 备注：预览服务器(8080, task 2GDYKF)仍运行；但静态预览无后端数据，editor-panel 默认隐藏，需在游戏内选中节点/路段才可见。
